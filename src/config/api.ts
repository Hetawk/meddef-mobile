/**
 * Production API origin (no trailing slash). All request helpers use
 * `normalizeApiBase` in `@/api/client`, which trims slashes so paths join safely.
 */
export const API_BASE_URL = "https://meddef.ekddigital.com";
